<div class="container">

	<h1>Products</h1>
  
  	<div class="row">
  		<%= render @products %>
   	</div>
  	<br>

	<% if current_user && current_user.admin? %>
  		<button><%= link_to 'New Product', new_product_path %></button>
  	<% end %>
</div>